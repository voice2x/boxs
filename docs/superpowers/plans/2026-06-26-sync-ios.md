# iOS 数据同步改造实现计划（方案 A · iOS）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 iOS 端从"全量刷新 + fire-and-forget push"改造为"持久化发件箱 + 增量游标拉取 + drain→pull + 后台/联网同步",对接已实现并验证的后端 `/changes` + `/batch` 端点。

**Architecture:** 本地写即落盘到 `sync_outbox`(权威推送队列);`SyncEngine`(actor)每个周期先 drain(按实体批量 `/batch`,LWW 对账)后 pull(`/changes` 游标分页 upsert + 墓碑/归档);`NWPathMonitor` 联网即触发 + `BGTaskScheduler` 周期后台。客户端 UUID 已是主键。冲突由服务端 LWW 裁决,本地采纳返回的胜出版本。

**Tech Stack:** Swift · SwiftUI · GRDB (SQLite) · URLSession(async/await) · BackgroundTasks

**前置(已完成):** 后端 `/changes` + `/batch` 已实现、合并 `main`、15 测试通过 + 冒烟通过。spec:`docs/superpowers/specs/2026-06-26-ios-backend-sync-design.md`;后端计划:`docs/superpowers/plans/2026-06-26-sync-backend.md`。

**验证策略(重要):** 项目当前**无 XCTest target**。本计划验证 = 每 task 跑 `xcodebuild build`(编译) + 末尾的**手动测试清单**(模拟器运行)。`SyncEngine` 以协议注入(`SyncNetworking`)设计,便于将来加 test target 后补单测。**自动化单测本期不做**。

**编译命令(每个 task 末尾跑):**
```bash
cd /Users/huayang/Projects/boxs/platform/ios
xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -8
```
(若 scheme 名不是 `Boxs`,先 `xcodebuild -project Boxs.xcodeproj -list` 确认。)

**后端契约(写代码时严格遵守):**
- `POST /api/data/{expenses,habits,habits/checkins,todos}/batch` 入参 `{"changes":[<完整记录 snake_case,含 updated_at;deleted_at 非空=墓碑>]}`,出参 `[{"status":"applied"|"conflict","record":<完整记录>}]`。
- `GET /api/data/{...}/changes?cursor=<opaque>&limit=200` 出参 `{"items":[<完整记录>],"next_cursor":"<opaque>"|null}`。`cursor` 缺省 = 全量引导。
- habit_checkins 的 change 用自然键 `{habit_id, record_date, value, note, updated_at}`(无 id)。
- 客户端 UUID v4 = 主键(expenses/todos/habits);checkins 按自然键。
- LWW 按客户端 `updated_at`;本地写时即打 `updated_at = Date()`。

**GRDB 关键注意(避免 bug):**
- `TodoRecord` 有 `willUpdate` 钩子(每次 update 自动 `updatedAt = Date()`)。这会**破坏**同步回写——`applyTodo` 若用 `save()`(update 路径)会把服务端权威 `updatedAt` 改成本地 now()。故所有 `apply*`(**同步回写**)**必须用 delete-by-id + insert**(insert 不触发 willUpdate),保留服务端 `updatedAt`。
- 用户**本地编辑**路径仍走 `save()`(willUpdate 自动 bump updatedAt,利于 LWW)。
- `enqueue*` 在入队时把记录 `updatedAt` 打成 `Date()` 并回存,保证每个待发变更有新鲜时间戳。

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `Core/Database/AppDatabase.swift` | 新增 `v2_sync` 迁移(sync_outbox + sync_cursor) |
| `Core/Database/SyncOutbox.swift`(新) | 发件箱 GRDB 记录 + 入队/读取助手 |
| `Core/Database/SyncCursor.swift`(新) | 游标 GRDB 记录 + get/set 助手 |
| `Core/Network/DTOs.swift` | 加 `BatchRequest`/`ChangesResponse`/`BatchResult`;`HabitRecordDTO` 补 `updated_at` |
| `Core/Network/Mappers.swift` | 加 `formatISO8601`;各 `toChange()`;`HabitRecordMapper.toLocal` 补 `updated_at` |
| `Core/Network/Endpoints.swift` | 8 个 `/changes`(GET)+ `/batch`(POST)端点 |
| `Core/Network/SyncNetworking.swift`(新) | `protocol SyncNetworking`,APIClient 遵循(可测试 seam) |
| `Core/Network/SyncEngine.swift`(新) | actor:enqueue / drainOutbox / pull / sync |
| `Core/Network/BackgroundSync.swift`(新) | NWPathMonitor + BGTaskScheduler 接线 |
| `App/AppDelegate.swift` | 启动同步改 `SyncEngine`;注册后台任务 |
| `ViewModels/*.swift` | 本地写后改 `enqueue`;移除 `pushXxx`;`syncXxx`→`SyncEngine.sync()` |

---

## Task 1: GRDB 迁移 v2_sync(发件箱 + 游标表)

**Files:** Modify `platform/ios/Boxs/Core/Database/AppDatabase.swift`

- [ ] **Step 1: 在 `migrate()` 中 v1 闭包之后、`migrator.migrate(dbQueue)` 之前插入 v2 迁移**

```swift
        // v2: 同步发件箱 + 游标
        migrator.registerMigration("v2_sync") { db in
            try db.create(table: "sync_outbox") { t in
                t.column("id", .integer).primaryKey(autoIncrement: true)
                t.column("entity", .text).notNull()
                t.column("recordKey", .text).notNull()
                t.column("op", .text).notNull().defaults(to: "upsert")
                t.column("payload", .blob).notNull()
                t.column("attempts", .integer).notNull().defaults(to: 0)
                t.column("createdAt", .datetime).notNull()
                t.uniqueKey(["entity", "recordKey"])
            }
            try db.create(table: "sync_cursor") { t in
                t.column("entity", .text).primaryKey()
                t.column("cursor", .text)
                t.column("lastSyncedAt", .datetime)
            }
        }
```

- [ ] **Step 2: 编译**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -5
```
预期:`** BUILD SUCCEEDED **`。

- [ ] **Step 3: 提交**

```bash
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Database/AppDatabase.swift && git commit -m "feat(ios): GRDB v2_sync 迁移（sync_outbox + sync_cursor）"
```

---

## Task 2: SyncOutbox + SyncCursor GRDB 记录

**Files:** Create `platform/ios/Boxs/Core/Database/SyncOutbox.swift`, `platform/ios/Boxs/Core/Database/SyncCursor.swift`

- [ ] **Step 1: SyncOutbox.swift(全文)**

```swift
import Foundation
import GRDB

/// 同步发件箱：待推送的本地变更（推送权威源）
struct SyncOutbox: Codable, FetchableRecord, PersistableRecord {
    static let databaseTableName = "sync_outbox"

    var id: Int64?
    var entity: String       // "expenses" | "todos" | "habits" | "habit_checkins"
    var recordKey: String    // id 或 "habitId|recordDate"
    var op: String           // "upsert"
    var payload: Data        // 该实体 change 的 JSON 快照
    var attempts: Int
    var createdAt: Date
}

extension SyncOutbox {
    /// 入队：同一 (entity, recordKey) 只保留最新一条（先删后插）
    static func enqueue(_ db: Database, entity: String, recordKey: String, payload: Data) throws {
        try SyncOutbox
            .filter(Column("entity") == entity && Column("recordKey") == recordKey)
            .deleteAll(db)
        var row = SyncOutbox(id: nil, entity: entity, recordKey: recordKey, op: "upsert",
                             payload: payload, attempts: 0, createdAt: Date())
        try row.insert(db)
    }

    /// 取某实体待发(按 id 升序)
    static func pending(_ db: Database, entity: String, limit: Int) throws -> [SyncOutbox] {
        try SyncOutbox
            .filter(Column("entity") == entity)
            .order(Column("id").asc)
            .limit(limit)
            .fetchAll(db)
    }

    static let allEntities = ["expenses", "todos", "habits", "habit_checkins"]
}
```

- [ ] **Step 2: SyncCursor.swift(全文)**

```swift
import Foundation
import GRDB

/// 各实体最后消费的服务端游标
struct SyncCursor: Codable, FetchableRecord, PersistableRecord {
    static let databaseTableName = "sync_cursor"

    var entity: String
    var cursor: String?       // nil = 尚未引导(全量拉取)
    var lastSyncedAt: Date?
}

extension SyncCursor {
    static func get(_ db: Database, entity: String) throws -> String? {
        try SyncCursor.fetchOne(
            db,
            sql: "SELECT cursor FROM sync_cursor WHERE entity = ?",
            arguments: [entity]
        )
    }

    static func set(_ db: Database, entity: String, cursor: String?) throws {
        let row = SyncCursor(entity: entity, cursor: cursor, lastSyncedAt: Date())
        try row.save(db)
    }
}
```

- [ ] **Step 3: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -3
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Database/SyncOutbox.swift platform/ios/Boxs/Core/Database/SyncCursor.swift && git commit -m "feat(ios): SyncOutbox + SyncCursor GRDB 记录"
```

---

## Task 3: DTO + Mapper 同步扩展

**Files:** Modify `platform/ios/Boxs/Core/Network/DTOs.swift`, `platform/ios/Boxs/Core/Network/Mappers.swift`

- [ ] **Step 1: DTOs.swift — 通用信封 + HabitRecordDTO 补 updated_at**

在 `DTOs.swift` 顶部(import 后)加:

```swift
// MARK: - Sync 通用信封

struct BatchRequest<C: Encodable>: Encodable {
    let changes: [C]
}

struct ChangesResponse<D: Decodable>: Decodable {
    let items: [D]
    let next_cursor: String?
}

struct BatchResult<D: Decodable>: Decodable {
    let status: String   // "applied" | "conflict"
    let record: D
}
```

把 `HabitRecordDTO` 改为(后端现在返回 updated_at):

```swift
struct HabitRecordDTO: Decodable, Sendable {
    let id: String
    let habit_id: String
    let user_id: String
    let value: Double?
    let note: String?
    let record_date: String
    let created_at: String
    let updated_at: String?
}
```

- [ ] **Step 2: Mappers.swift — 加 formatISO8601**

在 `formatTimeHMS` 之后加:

```swift
/// Date → ISO8601 字符串 "2026-06-12T14:30:00Z"
func formatISO8601(_ date: Date) -> String {
    let f = ISO8601DateFormatter()
    f.formatOptions = [.withInternetDateTime]
    return f.string(from: date)
}
```

- [ ] **Step 3: Mappers.swift — 末尾追加各 batch change 类型与 toChange()**

```swift
// MARK: - Sync Batch Changes (本地 → /batch)

struct ExpenseChange: Encodable {
    let id: String
    let user_id: String
    let record_type: String
    let amount_cents: Int
    let category: String
    let note: String?
    let record_date: String
    let deleted_at: String?
    let created_at: String
    let updated_at: String?
}

extension ExpenseMapper {
    static func toChange(_ r: ExpenseRecord) -> ExpenseChange {
        ExpenseChange(
            id: r.id, user_id: r.userId, record_type: r.type, amount_cents: r.amountCents,
            category: r.category, note: r.note, record_date: formatDateYMD(r.recordDate),
            deleted_at: r.deletedAt.map { formatISO8601($0) },
            created_at: formatISO8601(r.createdAt),
            updated_at: r.updatedAt.map { formatISO8601($0) }
        )
    }
}

struct HabitChange: Encodable {
    let id: String
    let user_id: String
    let name: String
    let emoji: String?
    let frequency: String
    let target_value: Double?
    let unit: String?
    let is_active: Bool
    let created_at: String
    let updated_at: String?
}

extension HabitMapper {
    static func toChange(_ r: HabitDefinition) -> HabitChange {
        HabitChange(
            id: r.id, user_id: r.userId, name: r.name, emoji: r.icon,
            frequency: "daily", target_value: r.goalValue.flatMap { Double($0) },
            unit: r.unit, is_active: r.isActive,
            created_at: formatISO8601(r.createdAt),
            updated_at: r.updatedAt.map { formatISO8601($0) }
        )
    }
}

struct TodoChange: Encodable {
    let id: String
    let user_id: String
    let title: String
    let note: String?
    let due_date: String?
    let due_time: String?
    let priority: String?
    let status: String
    let completed_at: String?
    let deleted_at: String?
    let created_at: String
    let updated_at: String?
}

extension TodoMapper {
    static func toChange(_ r: TodoRecord) -> TodoChange {
        TodoChange(
            id: r.id, user_id: r.userId, title: r.content, note: nil,
            due_date: r.remindAt.map { formatDateYMD($0) },
            due_time: r.remindAt.map { formatTimeHMS($0) },
            priority: r.priority,
            status: r.isCompleted ? "completed" : "pending",
            completed_at: r.completedAt.map { formatISO8601($0) },
            deleted_at: r.deletedAt.map { formatISO8601($0) },
            created_at: formatISO8601(r.createdAt),
            updated_at: r.updatedAt.map { formatISO8601($0) }
        )
    }
}

struct CheckinChange: Encodable {
    let habit_id: String
    let record_date: String
    let value: Double?
    let note: String?
    let updated_at: String
}

extension HabitRecordMapper {
    static func toChange(_ r: HabitRecord) -> CheckinChange {
        CheckinChange(
            habit_id: r.habitId,
            record_date: formatDateYMD(r.recordDate),
            value: r.value.flatMap { Double($0) },
            note: nil,
            updated_at: formatISO8601(r.updatedAt ?? r.createdAt)
        )
    }
}
```

- [ ] **Step 4: Mappers.swift — HabitRecordMapper.toLocal 补 updated_at**

```swift
    static func toLocal(_ dto: HabitRecordDTO) -> HabitRecord {
        HabitRecord(
            id: dto.id,
            userId: dto.user_id,
            habitId: dto.habit_id,
            value: dto.value.map { String($0) },
            recordDate: DateFormatter.yyyyMMdd.date(from: dto.record_date) ?? Date(),
            createdAt: parseISO8601(dto.created_at) ?? Date(),
            updatedAt: dto.updated_at.flatMap { parseISO8601($0) }
        )
    }
```

- [ ] **Step 5: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -3
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Network/DTOs.swift platform/ios/Boxs/Core/Network/Mappers.swift && git commit -m "feat(ios): 同步 DTO/BatchChange + HabitRecord updated_at"
```

---

## Task 4: Endpoints — /changes + /batch

**Files:** Modify `platform/ios/Boxs/Core/Network/Endpoints.swift`

- [ ] **Step 1: 在 `Endpoints` enum 的 Todos 区块后(`completeTodo` 之后)追加**

```swift
    // MARK: - Sync (/changes + /batch)
    static func expenseChanges(cursor: String?, limit: Int = 200) -> Endpoint {
        var q: [URLQueryItem] = [URLQueryItem(name: "limit", value: "\(limit)")]
        if let cursor { q.append(URLQueryItem(name: "cursor", value: cursor)) }
        return Endpoint(method: .GET, path: "/api/data/expenses/changes", queryItems: q)
    }
    static let expenseBatch = Endpoint(method: .POST, path: "/api/data/expenses/batch")

    static func habitChanges(cursor: String?, limit: Int = 200) -> Endpoint {
        var q: [URLQueryItem] = [URLQueryItem(name: "limit", value: "\(limit)")]
        if let cursor { q.append(URLQueryItem(name: "cursor", value: cursor)) }
        return Endpoint(method: .GET, path: "/api/data/habits/changes", queryItems: q)
    }
    static let habitBatch = Endpoint(method: .POST, path: "/api/data/habits/batch")

    static func checkinChanges(cursor: String?, limit: Int = 200) -> Endpoint {
        var q: [URLQueryItem] = [URLQueryItem(name: "limit", value: "\(limit)")]
        if let cursor { q.append(URLQueryItem(name: "cursor", value: cursor)) }
        return Endpoint(method: .GET, path: "/api/data/habits/checkins/changes", queryItems: q)
    }
    static let checkinBatch = Endpoint(method: .POST, path: "/api/data/habits/checkins/batch")

    static func todoChanges(cursor: String?, limit: Int = 200) -> Endpoint {
        var q: [URLQueryItem] = [URLQueryItem(name: "limit", value: "\(limit)")]
        if let cursor { q.append(URLQueryItem(name: "cursor", value: cursor)) }
        return Endpoint(method: .GET, path: "/api/data/todos/changes", queryItems: q)
    }
    static let todoBatch = Endpoint(method: .POST, path: "/api/data/todos/batch")
```

- [ ] **Step 2: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -3
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Network/Endpoints.swift && git commit -m "feat(ios): /changes + /batch 端点定义"
```

---

## Task 5: SyncNetworking 协议(可测试 seam)

**Files:** Create `platform/ios/Boxs/Core/Network/SyncNetworking.swift`

- [ ] **Step 1: 全文**

```swift
import Foundation

/// 同步层网络抽象,便于将来注入 mock 做单测
protocol SyncNetworking: Sendable {
    func request<T: Decodable>(_ endpoint: Endpoint, body: Encodable?) async throws -> T
}

extension APIClient: SyncNetworking {}
```

> `APIClient.request` 现有签名为 `func request<T: Decodable>(_ endpoint: Endpoint, body: Encodable? = nil) async throws -> T`,满足协议要求。

- [ ] **Step 2: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -3
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Network/SyncNetworking.swift && git commit -m "feat(ios): SyncNetworking 协议(可测试 seam)"
```

---

## Task 6: SyncEngine 核心(actor)

**Files:** Create `platform/ios/Boxs/Core/Network/SyncEngine.swift`

- [ ] **Step 1: SyncEngine.swift(全文)**

```swift
import Foundation
import GRDB
import os.log

/// 同步引擎:本地写 → 发件箱;周期 = drain(推送) → pull(拉取)。
/// 冲突由服务端 LWW 裁决;本地采纳返回的胜出版本。
actor SyncEngine {
    static let shared = SyncEngine()

    private let api: SyncNetworking
    private let logger = Logger(subsystem: "com.boxs.app", category: "SyncEngine")
    private let batchSize = 50
    private let maxAttempts = 10
    private let minInterval: TimeInterval = 30
    private var lastSyncAt: Date = .distantPast
    private var isSyncing = false

    init(api: SyncNetworking = APIClient.shared) { self.api = api }

    // MARK: - 入队（本地写后调用）

    func enqueueExpense(_ r: ExpenseRecord) async {
        var rec = r; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("expenses", key: rec.id, ExpenseMapper.toChange(rec))
    }
    func enqueueExpenseDelete(id: String) async {
        guard var rec = await fetch(ExpenseRecord.self, id: id) else { return }
        rec.deletedAt = Date(); rec.isDeleted = true; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("expenses", key: rec.id, ExpenseMapper.toChange(rec))
    }
    func enqueueTodo(_ r: TodoRecord) async {
        var rec = r; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("todos", key: rec.id, TodoMapper.toChange(rec))
    }
    func enqueueTodoDelete(id: String) async {
        guard var rec = await fetch(TodoRecord.self, id: id) else { return }
        rec.deletedAt = Date(); rec.isDeleted = true; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("todos", key: rec.id, TodoMapper.toChange(rec))
    }
    func enqueueHabit(_ r: HabitDefinition) async {
        var rec = r; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("habits", key: rec.id, HabitMapper.toChange(rec))
    }
    func enqueueHabitArchive(id: String) async {
        guard var rec = await fetch(HabitDefinition.self, id: id) else { return }
        rec.isActive = false; rec.updatedAt = Date()
        await saveLocal(rec)
        await enqueue("habits", key: rec.id, HabitMapper.toChange(rec))
    }
    func enqueueCheckin(_ r: HabitRecord) async {
        var rec = r; rec.updatedAt = Date()
        await saveLocal(rec)
        let key = "\(rec.habitId)|\(formatDateYMD(rec.recordDate))"
        await enqueue("habit_checkins", key: key, HabitRecordMapper.toChange(rec))
    }

    private func enqueue<C: Encodable>(_ entity: String, key: String, _ change: C) async {
        guard let payload = try? JSONEncoder.boxs.encode(change) else { return }
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { try SyncOutbox.enqueue($0, entity: entity, recordKey: key, payload: payload) }
    }

    // MARK: - 本地读写助手

    private func saveLocal<T: PersistableRecord & FetchableRecord>(_ rec: T) async where T: MutablePersistableRecord {
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { try rec.save($0) }
    }
    private func fetch<T: FetchableRecord & PersistableRecord>(_ type: T.Type, id: String) async -> T? where T: MutablePersistableRecord {
        guard let db = try? AppDatabase.shared.getDB() else { return nil }
        return try? await db.read { try T.fetchOne($0, key: id) }
    }

    // MARK: - drain(推送)

    func drainOutbox() async {
        await drainExpenses(); await drainTodos(); await drainHabits(); await drainCheckins()
    }

    private func drainExpenses() async {
        guard let (rows, changes) = await collect("expenses", as: ExpenseChange.self), !changes.isEmpty else { return }
        do {
            let results: [BatchResult<ExpenseDTO>] = try await api.request(Endpoints.expenseBatch, body: BatchRequest(changes: changes))
            await reconcile(rows, results) { await self.applyExpense($0) }
        } catch {
            await bumpAttempts(ids: rows.map { $0.id }); logger.warning("drain expenses 失败: \(error.localizedDescription)")
        }
    }
    private func drainTodos() async {
        guard let (rows, changes) = await collect("todos", as: TodoChange.self), !changes.isEmpty else { return }
        do {
            let results: [BatchResult<TodoDTO>] = try await api.request(Endpoints.todoBatch, body: BatchRequest(changes: changes))
            await reconcile(rows, results) { await self.applyTodo($0) }
        } catch {
            await bumpAttempts(ids: rows.map { $0.id }); logger.warning("drain todos 失败: \(error.localizedDescription)")
        }
    }
    private func drainHabits() async {
        guard let (rows, changes) = await collect("habits", as: HabitChange.self), !changes.isEmpty else { return }
        do {
            let results: [BatchResult<HabitDefinitionDTO>] = try await api.request(Endpoints.habitBatch, body: BatchRequest(changes: changes))
            await reconcile(rows, results) { await self.applyHabit($0) }
        } catch {
            await bumpAttempts(ids: rows.map { $0.id }); logger.warning("drain habits 失败: \(error.localizedDescription)")
        }
    }
    private func drainCheckins() async {
        guard let (rows, changes) = await collect("habit_checkins", as: CheckinChange.self), !changes.isEmpty else { return }
        do {
            let results: [BatchResult<HabitRecordDTO>] = try await api.request(Endpoints.checkinBatch, body: BatchRequest(changes: changes))
            await reconcile(rows, results) { await self.applyCheckin($0) }
        } catch {
            await bumpAttempts(ids: rows.map { $0.id }); logger.warning("drain checkins 失败: \(error.localizedDescription)")
        }
    }

    /// 取某实体一批待发,payload 解码失败的行整体跳过(保持 rows 与 changes 一一对齐)
    private func collect<C: Decodable>(_ entity: String, as: C.Type) async -> ([SyncOutbox], [C])? {
        guard let db = try? AppDatabase.shared.getDB() else { return nil }
        let rows: [SyncOutbox]
        do { rows = try await db.read { try SyncOutbox.pending($0, entity: entity, limit: batchSize) } }
        catch { return nil }
        var out: [SyncOutbox] = []; var changes: [C] = []
        for r in rows {
            guard let c = try? JSONDecoder.boxs.decode(C.self, from: r.payload) else { continue }
            out.append(r); changes.append(c)
        }
        return (out, changes)
    }

    /// 逐条把服务端返回(胜出版本)写回本地,删除对应 outbox 行
    private func reconcile<D>(_ rows: [SyncOutbox], _ results: [BatchResult<D>], apply: (D) async -> Void) async {
        for (i, row) in rows.enumerated() where i < results.count {
            await apply(results[i].record)        // applied 与 conflict 都采纳服务端版本
            await deleteOutbox(id: row.id)
        }
    }

    private func deleteOutbox(id: Int64?) async {
        guard let id, let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { try SyncOutbox.deleteOne($0, key: id) }
    }
    private func bumpAttempts(ids: [Int64?]) async {
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { db in
            for id in ids.compactMap({ $0 }) {
                guard var r = try SyncOutbox.fetchOne(db, key: id) else { continue }
                r.attempts += 1
                if r.attempts >= maxAttempts { try SyncOutbox.deleteOne(db, key: id) }
                else { try r.update(db) }
            }
        }
    }

    // MARK: - apply(同步回写:delete+insert,绕过 willUpdate,保留服务端 updatedAt)

    private func applyExpense(_ dto: ExpenseDTO) async { await replaceLocal(ExpenseMapper.toLocal(dto), id: dto.id) }
    private func applyTodo(_ dto: TodoDTO) async { await replaceLocal(TodoMapper.toLocal(dto), id: dto.id) }
    private func applyHabit(_ dto: HabitDefinitionDTO) async { await replaceLocal(HabitMapper.toLocal(dto), id: dto.id) }
    private func applyCheckin(_ dto: HabitRecordDTO) async {
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { db in
            let rec = HabitRecordMapper.toLocal(dto)
            try HabitRecord
                .filter(Column("habitId") == rec.habitId && Column("recordDate") == rec.recordDate)
                .deleteAll(db)
            try rec.insert(db)
        }
    }
    /// 按 id 删旧 + insert 服务端记录(insert 不触发 willUpdate,保留 updatedAt)
    private func replaceLocal<T: MutablePersistableRecord>(_ rec: T, id: String) async {
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { db in
            try db.execute(sql: "DELETE FROM \(T.databaseTableName) WHERE id = ?", arguments: [id])
            try rec.insert(db)
        }
    }

    // MARK: - pull(增量)

    func pullAll() async {
        await pullExpenses(); await pullHabits(); await pullCheckins(); await pullTodos()
    }
    private func pullExpenses() async {
        await pullLoop(entity: "expenses", endpoint: { Endpoints.expenseChanges(cursor: $0) }, apply: { await self.applyExpense($0) })
    }
    private func pullTodos() async {
        await pullLoop(entity: "todos", endpoint: { Endpoints.todoChanges(cursor: $0) }, apply: { await self.applyTodo($0) })
    }
    private func pullHabits() async {
        await pullLoop(entity: "habits", endpoint: { Endpoints.habitChanges(cursor: $0) }, apply: { await self.applyHabit($0) })
    }
    private func pullCheckins() async {
        await pullLoop(entity: "habit_checkins", endpoint: { Endpoints.checkinChanges(cursor: $0) }, apply: { await self.applyCheckin($0) })
    }

    /// 翻页拉取直到 next_cursor == nil;每页 upsert 后存新游标
    private func pullLoop<D: Decodable>(
        entity: String,
        endpoint: (String?) -> Endpoint,
        apply: @escaping (D) async -> Void
    ) async {
        var cursor = await readCursor(entity)
        repeat {
            let resp: ChangesResponse<D>
            do { resp = try await api.request(endpoint(cursor)) }
            catch { logger.warning("pull \(entity) 失败: \(error.localizedDescription)"); return }
            for item in resp.items { await apply(item) }
            cursor = resp.next_cursor
            await writeCursor(entity, cursor)
        } while cursor != nil
    }

    private func readCursor(_ entity: String) async -> String? {
        guard let db = try? AppDatabase.shared.getDB() else { return nil }
        return try? await db.read { try SyncCursor.get($0, entity: entity) }
    }
    private func writeCursor(_ entity: String, _ cursor: String?) async {
        guard let db = try? AppDatabase.shared.getDB() else { return }
        _ = try? await db.write { try SyncCursor.set($0, entity: entity, cursor: cursor) }
    }

    // MARK: - 周期入口

    func sync() async {
        guard TokenManager.shared.isLoggedIn else { return }
        if isSyncing { return }
        if Date().timeIntervalSince(lastSyncAt) < minInterval { return }
        isSyncing = true
        defer { isSyncing = false; lastSyncAt = Date() }
        logger.info("sync 周期开始")
        await drainOutbox()
        await pullAll()
        logger.info("sync 周期完成")
    }
}
```

> 注:`saveLocal`/`fetch`/`replaceLocal` 均为私有泛型助手,以具体记录类型(ExpenseRecord 等)调用,约束按编译器提示精简即可。`replaceLocal` 用裸 SQL 删旧行 + `insert`(insert 不触发 `willUpdate`),保留服务端 `updatedAt`;`T.databaseTableName` 由 `PersistableRecord` 提供。

- [ ] **Step 2: 编译(最大、最易出错的一步,据报错修正 actor 隔离 / 泛型约束)**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -10
```
预期:`** BUILD SUCCEEDED **`。常见修法:方法引用改显式闭包;泛型约束按编译器提示精简;`Sendable` 警告可加 `@Sendable` 或 `nonisolated(unsafe)`。

- [ ] **Step 3: 提交**

```bash
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Network/SyncEngine.swift && git commit -m "feat(ios): SyncEngine（发件箱 drain + 增量 pull + LWW 对账）"
```

---

## Task 7: 改 ViewModel / AppDelegate 接入 SyncEngine

**Files:** Modify `App/AppDelegate.swift`, `ViewModels/HomeViewModel.swift`, `ViewModels/TodoViewModel.swift`, `ViewModels/HabitViewModel.swift`, `ViewModels/NLUViewModel.swift`

> 原则:本地写库后 `await SyncEngine.shared.enqueueXxx(record)` 入队,再 `Task { await SyncEngine.shared.sync() }` 触发。`syncXxx()` 全部换为 `SyncEngine.shared.sync()`。删除 = 入队带墓碑的完整记录(`enqueueXxxDelete`)。

- [ ] **Step 1: AppDelegate.swift**

把启动同步:
```swift
        if TokenManager.shared.isLoggedIn {
            Task { await SyncService.shared.syncAll() }
        }
```
改为:
```swift
        if TokenManager.shared.isLoggedIn {
            Task { await SyncEngine.shared.sync() }
        }
        BackgroundSync.shared.register()
```
并在类中加:
```swift
    func applicationDidEnterBackground(_ application: UIApplication) {
        BackgroundSync.shared.scheduleAppRefresh()
    }
```

- [ ] **Step 2: HomeViewModel.swift**

`await SyncService.shared.syncAll()` → `await SyncEngine.shared.sync()`

- [ ] **Step 3: TodoViewModel.swift**

- `await SyncService.shared.syncTodos()` → `await SyncEngine.shared.sync()`
- 完成:`Task { await SyncService.shared.pushTodoComplete(id: id) }` → 本地把该待办置 `isCompleted=true/completedAt=now/updatedAt=now` 并 upsert,再:
  ```swift
  Task { await SyncEngine.shared.enqueueTodo(updatedRecord); await SyncEngine.shared.sync() }
  ```
- 删除 `:82`:`Task { await SyncService.shared.pushTodoDelete(id: id) }` →
  ```swift
  Task { await SyncEngine.shared.enqueueTodoDelete(id: id); await SyncEngine.shared.sync() }
  ```

- [ ] **Step 4: HabitViewModel.swift**

- `:28` `syncHabits()` → `await SyncEngine.shared.sync()`
- `:63` `pushHabitCheckin(record)` → `await SyncEngine.shared.enqueueCheckin(record)` + `Task { await SyncEngine.shared.sync() }`

- [ ] **Step 5: NLUViewModel.swift**

- `:127` `pushExpense(record)` → `await SyncEngine.shared.enqueueExpense(record)` + `Task { await SyncEngine.shared.sync() }`
- `:145` `pushHabitCheckin(record)` → `await SyncEngine.shared.enqueueCheckin(record)` + `sync()`
- `:158` `pushTodo(record)` → `await SyncEngine.shared.enqueueTodo(record)` + `sync()`

- [ ] **Step 6: 旧 SyncService 处置**

执行 Step 1–5 后:
```bash
cd /Users/huayang/Projects/boxs/platform/ios && grep -rn "SyncService\|pushExpense\|pushTodo\|pushHabit\|syncAll\|syncTodos\|syncHabits" Boxs --include=*.swift
```
若结果只剩 `SyncService.swift` 自身 → **删除** `Core/Network/SyncService.swift`(并从 Xcode 工程移除引用)。若仍有外部引用 → 把 `SyncService` 改为转发 shim 到 `SyncEngine` 再逐步清理。

- [ ] **Step 7: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -8
cd /Users/huayang/Projects/boxs && git add -A platform/ios/Boxs && git commit -m "feat(ios): ViewModel/AppDelegate 接入 SyncEngine,移除 fire-and-forget push"
```

> ⚠️ 删除 `SyncService.swift` 需同步从 `.xcodeproj/project.pbxproj` 移除其引用,否则编译报缺文件。最稳:用 Xcode 删除(移到回收站);或手改 pbxproj 删除对应 PBXFileReference/PBXBuildFile 行。新增的 4 个 `.swift` 文件(SyncOutbox/SyncCursor/SyncEngine/SyncNetworking/BackgroundSync)同理需加入 target —— **Xcode 一般会自动纳入同 target 的新文件,但纯文件系统新增需在 pbxproj 注册**。若 build 报 "cannot find 'SyncEngine' in scope",需把这些文件加入 Boxs target 的 Compile Sources。

---

## Task 8: 后台同步(NWPathMonitor + BGTaskScheduler)

**Files:** Create `platform/ios/Boxs/Core/Network/BackgroundSync.swift`;Modify `platform/ios/Boxs/Info.plist`

- [ ] **Step 1: BackgroundSync.swift(全文)**

```swift
import Foundation
import Network
import BackgroundTasks

/// 联网即触发 drain + 周期后台同步
final class BackgroundSync {
    static let shared = BackgroundSync()
    static let bgTaskIdentifier = "com.boxs.app.sync"
    private let monitor = NWPathMonitor()
    private let queue = DispatchQueue(label: "com.boxs.app.network")
    private init() {}

    func register() {
        startNetworkMonitor()
        BGTaskScheduler.shared.register(forTaskWithIdentifier: BackgroundSync.bgTaskIdentifier, using: nil) { task in
            guard let task = task as? BGAppRefreshTask else { return }
            self.handleAppRefresh(task)
        }
    }

    private func startNetworkMonitor() {
        monitor.pathUpdateHandler = { path in
            guard path.status == .satisfied else { return }
            Task { await SyncEngine.shared.sync() }
        }
        monitor.start(queue: queue)
    }

    func scheduleAppRefresh() {
        let request = BGAppRefreshTaskRequest(identifier: BackgroundSync.bgTaskIdentifier)
        request.earliestBeginDate = Date(timeIntervalSinceNow: 60 * 15)
        try? BGTaskScheduler.shared.submit(request)
    }

    private func handleAppRefresh(_ task: BGAppRefreshTask) {
        scheduleAppRefresh()
        let op = Task { await SyncEngine.shared.sync() }
        task.expirationHandler = { op.cancel() }
        Task {
            await op.value
            task.setTaskCompleted(success: true)
        }
    }
}
```

- [ ] **Step 2: Info.plist 注册后台任务**

在 `platform/ios/Boxs/Info.plist` 顶层 `<dict>` 内加(若 `UIBackgroundModes` 已存在则合并):
```xml
<key>BGTaskSchedulerPermittedIdentifiers</key>
<array>
    <string>com.boxs.app.sync</string>
</array>
<key>UIBackgroundModes</key>
<array>
    <string>fetch</string>
    <string>processing</string>
</array>
```

- [ ] **Step 3: 编译 + 提交**

```bash
cd /Users/huayang/Projects/boxs/platform/ios && xcodebuild -project Boxs.xcodeproj -scheme Boxs -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -5
cd /Users/huayang/Projects/boxs && git add platform/ios/Boxs/Core/Network/BackgroundSync.swift platform/ios/Boxs/Info.plist && git commit -m "feat(ios): 后台同步（NWPathMonitor + BGTaskScheduler）"
```

---

## Task 9: 手动测试清单(模拟器运行验证)

> 无 XCTest target,以手动验证为准。后端用已验证实例(`DATABASE_URL=postgres://myuser:mypassword@115.191.21.194:7458/mydb` 启服务),或把 `AppConfiguration.apiBaseURL` 指向可达的后端。

- [ ] **Step 1: 首次全量引导** — 登录后日志 `sync 周期完成`;`sync_cursor` 各 entity 游标从 nil 变非空;本地能查到后端历史数据。
- [ ] **Step 2: 本地写→推送** — 新建记账 → `sync_outbox` 出现一行 → 联网 drain → outbox 清空 → 后端 `/changes` 能拉到(客户端 UUID 作 id)。
- [ ] **Step 3: 断网写→恢复补推** — 飞行模式新建待办 → outbox 留存 → 关飞行 → NWPathMonitor 触发 → 补推成功;断网期间 kill App 重开仍能补推(持久化)。
- [ ] **Step 4: LWW 冲突** — 同记录 A 改 amount=100(T1)、B 改=200(T2>T1);无论谁先 sync,最终两端都=200(B 胜)。
- [ ] **Step 5: 增量游标** — 二次 `sync()` 无新数据;后端改动后下次只拉增量。
- [ ] **Step 6: 删除传播** — 删除记账 → outbox 带墓碑 → drain → 后端 deleted_at 置位 → 另一设备 pull 后本地也标记删除。
- [ ] **Step 7: 后台同步** — 进入后台 15min+ 或 `e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@"com.boxs.app.sync"]` 触发后台 sync。

---

## 自检清单(执行前确认)

- 后端契约:`/batch` 入参=完整记录(含 `updated_at`、`deleted_at` 墓碑);`/changes` 出参 `next_cursor`;checkins 自然键。→ DTOs/Mappers 已对齐。
- 本地记录 id 已是客户端 UUID;checkins 按自然键对账(applyCheckin 删自然键旧行 + insert)。
- **同步回写用 delete+insert**(绕过 `TodoRecord.willUpdate`),保留服务端 `updatedAt`;用户本地编辑走 `save()`(willUpdate 自动 bump)。
- `enqueue*` 入队前把 `updatedAt=Date()` 回存,保证新鲜时间戳。
- drain→pull 固定顺序;死信(attempts≥10)丢弃。
- 401 走 APIClient 现有 token 刷新;未登录 `sync()` 直接 return。
- 后台任务标识 `com.boxs.app.sync` 在 Info.plist 注册。
- 新增 `.swift` 文件需加入 Boxs target 的 Compile Sources(pbxproj);删除 `SyncService.swift` 需同步移除引用。
- `HabitRecordDTO` 与 `HabitRecordMapper.toLocal` 已补 `updated_at`。
